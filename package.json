{
  "name": "vecta",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "pnpm --recursive build",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm test:operations && pnpm build && pnpm verify:bundle && pnpm verify:assistant && pnpm verify:no-secret-in-url && pnpm verify:workflow-pins && pnpm verify:release-age",
    "verify:bundle": "node .github/scripts/verify-client-bundle.mjs",
    "lint": "pnpm --recursive --if-present typegen && eslint .",
    "test": "pnpm --recursive test",
    "test:operations": "node --test .github/scripts/*.test.mjs",
    "typecheck": "pnpm --recursive typecheck",
    "types:worker": "pnpm --recursive --filter './apps/**' --if-present types:worker",
    "verify:assistant": "node .github/scripts/verify-assistant-boundary.mjs",
    "deploy:staging": "node .github/scripts/deploy-staging.mjs",
    "staging:secrets": "node .github/scripts/set-staging-secrets.mjs",
    "verify:no-secret-in-url": "node .github/scripts/verify-no-secret-in-url.mjs",
    "verify:workflow-pins": "node .github/scripts/verify-workflow-pins.mjs",
    "verify:release-age": "node .github/scripts/verify-release-age-policy.mjs",
    "audit:deps": "pnpm audit --prod --audit-level high",
    "security:surfaces": "node .github/scripts/security-surfaces.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.7.0",
    "eslint-plugin-security": "^4.0.1",
    "typescript": "7.0.2",
    "typescript-eslint": "8.65.0"
  }
}
